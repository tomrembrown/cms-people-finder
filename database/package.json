{
  "name": "database",
  "version": "0.0.1",
  "description": "The database for the cms people finder - just scripts to the create the database",
  "main": "createDatabase.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "dotenv": "^10.0.0",
    "pg": "^8.6.0"
  },
  "scripts": {
    "createTables": "node ./createTables.js"
  }
}
